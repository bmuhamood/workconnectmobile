// index.js
// Plain expo-router entry — Firebase push notifications are paused for
// now (see README.md "Push notifications" section) so this app can run
// in plain Expo Go for fast testing. When Firebase is added back later,
// this file needs a background message handler registered here again —
// see supabase/FIREBASE_SETUP.md and git history for the previous version.
import 'expo-router/entry';
